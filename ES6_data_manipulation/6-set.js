export default function setFromArray(array) {
  return new Set(array);
}

// the "set" method allows you to use the "new" keyword to initiate the constructor method for the "set" class. When finalized it will uniquely set all the values in a row removing any duplicates.
// "Arrays" can have duplicates whereas "Sets" cannot. 
