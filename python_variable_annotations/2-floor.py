#!/usr/bin/env python3
def floor(n: float) -> int:
    """
    Returns the floor of a float (largest integer less than or equal to n).
    
    Args:
        n: A floating point number
        
    Returns:
        The floor of n as an integer
    """
    import math
    return math.floor(n)